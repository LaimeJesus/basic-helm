apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-test.fullname" . }}
  labels:
    {{- include "basic-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basic-test.selectorLabels" . | nindent 4 }}
