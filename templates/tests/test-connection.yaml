apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basic-test.fullname" . }}-test-connection"
  labels:
    {{- include "basic-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "basic-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
