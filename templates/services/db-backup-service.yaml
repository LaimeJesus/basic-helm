apiVersion: v1
kind: Service
metadata:
    labels:
        io.kompose.service: db-backup
    name: db-backup
    namespace: kobotoolbox
spec:
    selector:
        io.kompose.service: db-backup
status: {}
